# Pre-existing non-concurrent CREATE INDEX statements that were already
# applied to production before scripts/check-migration-safety.sh gained the
# CREATE INDEX check. Grandfathered so history doesn't turn CI red.
#
# This list is closed. Do not add new entries for new migrations — a new
# hot-table index should be reviewed on its own merits (and documented
# inline in the migration if accepted) or built out-of-band per
# docs/hot-table-index-migrations.md, not added here.
#
# Format: <migration-filename>:<table-name>
#
# Six of these were flagged directly by issue audit evidence; the rest were
# found by running this check's new rule against the full migrations/
# directory (see docs/hot-table-index-migrations.md for the
# incident-correlation audit status of all ten).
20260220000000_settlements.sql:transactions
20260222000000_transaction_search_indexes.sql:transactions
20260426000001_feature_flag_rollout.sql:feature_flags
20260427000000_optimized_search_indexes.sql:transactions
20260428000002_feature_flag_dependencies.sql:feature_flags
20260501000000_tenant_rls.sql:transactions
20260526000000_add_trace_id.sql:transactions
20260601000000_webhook_exactly_once_delivery.sql:webhook_deliveries
20260620000000_add_horizon_payment_id.sql:transactions
20260823000004_webhook_delivery_dlq_unique_delivery_id.sql:webhook_delivery_dlq

# Pre-existing RENAME TO operations grandfathered for the same reason as the
# index entries above: already applied to production before this check existed.
# Format: <migration-filename>:RENAME_TO
#
# This migration renames the original transactions table as part of a
# coordinated table-swap to convert it to a partitioned table. The rename and
# the new table creation happen atomically in the same migration; no running
# app version was exposed to a missing table.
20250217000000_partition_transactions.sql:RENAME_TO
# This migration renames api_key to api_key_hash as part of a coordinated
# credential-hashing migration with an explicit precondition guard (RAISE
# EXCEPTION) — the rename and the application cutover are deployed together.
20260824000003_hash_tenant_secrets.sql:RENAME_COLUMN
