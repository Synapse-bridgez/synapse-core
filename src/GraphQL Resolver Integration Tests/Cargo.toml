[package]
name = "graphql-integration-tests"
version = "0.1.0"
edition = "2021"

[dependencies]
async-graphql = "7.0"
tokio = { version = "1.0", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
tokio-test = "0.4"
